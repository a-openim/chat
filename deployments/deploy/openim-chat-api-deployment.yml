apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-api-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: chat-api-server
  template:
    metadata:
      labels:
        app: chat-api-server
    spec:
      containers:
        - name: openim-chat-api-container
          image: 10.88.88.13:81/app/openim-chat-api:v260202075751
          imagePullPolicy: IfNotPresent
          env:
            - name: CONFIG_PATH
              value: "/config"
            - name: CHATENV_REDIS_PASSWORD
              value: "root123456"
            - name: IMENV_MONGODB_USERNAME
              value: "root"
            - name: CHATENV_MONGODB_PASSWORD
              value: "root123456"

          volumeMounts:
            - name: openim-chat-config
              mountPath: "/config"
              readOnly: true
          ports:
            - containerPort: 10008
      volumes:
        - name: openim-chat-config
          configMap:
            name: openim-chat-config
