apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-rpc-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin-rpc-server
  template:
    metadata:
      labels:
        app: admin-rpc-server
    spec:
      containers:
        - name: openim-admin-rpc-container
          image: 10.88.88.13:81/app/openim-admin-rpc:v260131094041
          env:
            - name: CONFIG_PATH
              value: "/config"
            - name: CHATENV_REDIS_PASSWORD
              value: "root123456"
            - name: IMENV_MONGODB_USERNAME
              value: "root"
            - name: CHATENV_MONGODB_PASSWORD
              value: "root123456"

          volumeMounts:
            - name: openim-chat-config
              mountPath: "/config"
              readOnly: true
          ports:
            - containerPort: 30200
      volumes:
        - name: openim-chat-config
          configMap:
            name: openim-chat-config
